/**
 * for node
 * 配置相对路径
 * 格式 myUrl:'/myUrl'
 */

const routeUrl = {
	// 这里面放置路由路径 如 test: '/test'
	testUrl: {
		mainPath:'/testUrl', // 提示：这个必须和key同名
		about:'/about'
	}
};

module.exports = routeUrl;
